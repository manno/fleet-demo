apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: nginx
  namespace: fleet-local
spec:
  branch: main
  paths:
  - nginx
  repo: https://github.com/manno/fleet-demo
  targets:
  - clusterSelector: {}
